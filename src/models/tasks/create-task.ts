export type BodyParams = {
  title: string;
  time: string;
  description: string;
  status: string;
};

export type TaskType = {
  id: string;
  title: string;
  status: string;
  time: string;
  description: string;
};
